# OCAQ — Denetim Raporu, 4 Avqust 2026

> Bu sənəd bir oturumda aparılmış 4 paralel kod denetiminin + 5 analiz
> artifact-ının nəticələrini saxlayır. Məqsəd: tapıntılar söhbətdə itməsin.
>
> **Metod qeydi:** bütün kod iddiaları `fayl:sətir` ilə göstərilib və oxunaraq
> yoxlanılıb. Canlı sayta bu mühitdən çıxış yoxdur (org egress 403), build
> alınmır (`xlsx` paketi `cdn.sheetjs.com`-dan gəlir, host bağlıdır) — ona görə
> **heç bir davranış iddiası "canlıda yoxlanıldı" deyil**, yalnız kod səviyyəsi.

---

## 0. Bu oturumda TƏTBİQ EDİLƏN (qalanı yalnız tapıntıdır)

| Commit | Nə |
|---|---|
| `211973c` | `--ocaq-red` təyinatı + mobil 16px input + `viewport` export |
| `c5f0576` | Saha Nəzarət bal/P0 motoru + 21 test (kataloq boş) |
| `d2c1653` | Deploy sənədinin iki səhv məlumatı düzəldildi |

Aşağıdaki bölmələrin **qalan hamısı tətbiq EDİLMƏYİB** — tapıntı statusundadır.

---

## 1. 🔴 Deploy — kökü tapılan problem (BAĞLANDI)

Aylarla "canlıdadır" deyilən iş əslində canlıda deyildi.

- Vercel **Production Branch = `main`** — ayar heç vaxt səhv deyildi
  (yer: `Settings → Environments → Production → Branch Tracking`, `Settings → Git`-də DEYİL)
- Problem: **`main` 10 iyulda donmuşdu** (`e128b9e`). Bütün iş
  `feat/analytics-on-commercial` və `claude/*`-da yığılırdı → Vercel onları
  **Preview** kimi build edirdi. "Build yaşıldır" görüntüsü aldadıcı idi.
- Canlı yalnız **əl ilə "Promote to Production"** ilə yenilənirdi.
- **Həll (2026-08-04):** `main` fast-forward → avtomatik zəncir bərpa olundu.
  Vercel-də təsdiqləndi: `main` · Production · Ready.

**Qayda:** canlıya çıxacaq hər şey **`main`-ə** getməlidir.

### Açıq borc
- `scripts/migrate-prod.mjs` **repoda yoxdur** — prod DB-ni düzəldən alət yalnız
  lokal diskdə. `CLAUDE.md` §3 pozuntusu. Repoya alınmalıdır.
- CI yoxdur (`.github/` boş) → ROADMAP Faz 0 #1/#2 açıq.

---

## 2. 🔴 UI/Mobil (qismən TƏTBİQ EDİLDİ)

### Tətbiq edildi
- **`--ocaq-red` təyinatı itmişdi** → 19 faylda 145 istifadə, **32 düymə
  görünməz** (`bg-[var(--ocaq-red)]` + `text-white` = ağ üzərində ağ).
  Ən kritik: `vardiya-checklist/page.tsx:425` "Checklistı Göndər", üstəlik
  `bg-white/95` footer-in içində (`:414`). Növbə seçicisi (`:236,246`) seçili
  halı göstərmirdi → istifadəçi TƏRSİNİ görürdü. Dəyər `#C8102E`
  (kodda 137 yerdə hardcode; `#E11D48`: 0 yer). Regressiya: `278e1f6`-da var idi.
- **iOS fokus zoom-u** — ≤767px-də `input/select/textarea` 16px. Əvvəl düzəliş
  yalnız `sales-client.tsx:259`-da idi; 7 sistem 12–14px qalırdı.
- **`viewport` export-u** — `viewport-fit=cover` yox idi → `env(safe-area-inset-*)`
  0px-ə həll olunurdu, halbuki o kod artıq yazılmışdı.

### TƏTBİQ EDİLMƏYİB — mobil borc (prioritet sırası)
1. **5 modal yüksəklik sınırı olmadan kilitlənir** — `sales-client.tsx:753`,
   `:831`, `team-client.tsx:355`, `regions-client.tsx:243`, `staff-list.tsx:524`.
   `position:fixed` + `alignItems:center`, iç scroll yox → klaviatura açılanda
   göndər düyməsi əlçatmaz. Düzgün nümunə **artıq repoda**:
   `complaints-client.tsx:568` (`maxHeight:'calc(100dvh - 32px)'`),
   `branches-client.tsx:154`. `staff-list.tsx:519`-da backdrop bağlama yox.
2. **KXT qaralaması itir** — `vardiya-checklist/page.tsx:19` tamamilə `useState`.
   `localStorage`/`serviceWorker`/PWA **heç biri yox** (grep = 0). 100+ maddə +
   foto girildikdən sonra telefon kilidlənsə hamısı gedir. Müqayisə:
   `vardiya-liderliyi` server qaralaması dəstəkləyir (`:156`).
3. **Çifte scroll** — `vardiya-checklist/page.tsx:161` `min-h-[100dvh]`, halbuki
   `.dashboard-main` içindədir (`100vh − 100px`) → ~132px xəyal scroll +
   `:265` iç `overflow-y-auto`. **Risk: orta — real cihazda yoxlanmalıdır.**
4. **Kiçik toxunma hədəfləri** — ən pisi `vardiya-checklist/page.tsx:184`
   filial seçici ~22px (yanlış filiala KXT göndərmək əməliyyat xətasıdır).
   5 × 32px bağlama düyməsi, `:167` geri linki, sidebar nav ~35px.
5. **3 cədvəl kırpılır** — `ekipman/page.tsx:183`, `upload-flow.tsx:139`
   (ebeveyn `overflow:hidden` → sağ kolonlar ƏLÇATMAZ), `fire/page.tsx:164`.
   Qalan 13 cədvəldə `overflowX:'auto'` var, problem yox.

### Yaxşı olan (yoxlanıldı)
Sidebar mobil drawer düzgün işləyir (`globals.css:34-39`, `dashboard-shell.tsx`);
KPI paneli repodaki ən yaxşı responsive səhifə; **yalnız hover-a bağlı gizli
aksiyon YOXDUR**; foto axını `capture="environment"` ilə düşünülmüş.

---

## 3. 🎁 Promosyonlar — modul ERİŞİLƏ BİLMİR

**Ən kritik tapıntı:** `src/lib/dashboard-access.ts:8` — `/dashboard/promosyonlar`
**`LEGACY_MOCK_ROUTES`** siyahısındadır; `src/proxy.ts:39-40` hər rol üçün
`/dashboard`-a yönləndirir. `/admin/promosyonlar` da (`:17`).

→ Sidebar-dakı 🎁 link (`sidebar.tsx:14`) tıklananda İdarə panelinə atır.
→ `CHANGELOG.md` "mock-dan gerçəyə" deyir, FAZ3 sənədi "✅ qismən" deyir —
  **üçü də yanlışdır.** Modul deploy edilmiş, lakin heç kim aça bilmir.
  (Ehtimal: köhnə mock promo səhifəsi üçün qoyulmuş guard, yeni modul gələndə
  siyahıdan çıxarılmamış.)

### Açmadan əvvəl bağlanmalı
- **RBAC:** `GET /api/promotions` yalnız `if (!session)` yoxlayır
  (`route.ts:12`) → **`staff` daxil hər kəs** bütün kampaniyaları, kodlarla
  birlikdə API-dən çəkə bilər (`proxy.ts:47` matcher `api`-ni xaric edir).
- **`region_manager` şəbəkə geneline yayın edə bilir** — `branch_id` NULL
  qalanda promo 30 filiala görünür; bölgə sınırı yox.
- **IDOR:** `route.ts:55` `branch_id` gövdədən alınır, `canAccessBranch`
  çağırılmır → nəzəri cross-tenant. (Form hazırda göndərmir, endpoint açıqdır.)
- **Audit sıfır** — layihədə 6 route audit yazır, promo bir sətir yazmır.
  İndirim səlahiyyəti = pul səlahiyyəti.
- **Rate limit yox** (POST + şəkil yükləmə); ham `e.message` istemciye dönür.
- **Xəta udulur:** `dashboard/promosyonlar/page.tsx:35` `catch { rows = [] }` →
  DB xətası "Hələ promosyon yoxdur" ekranına çevrilir. `CLAUDE.md` §2.7 pozuntusu.

### Saxta/mock qalıqları
- **QR sахtadır** — `promo-client.tsx:100-109` kesikli çizgili qutu + 📱 emoji.
  `package.json`-da QR kitabxanası yox. Altındaki mətn (`:108`)
  *"Kassa bu kodu oxuyaraq endirimi tətbiq edir"* — **doğru deyil**, POS
  inteqrasiyası yoxdur, `code` iiko-da tanımlı deyil.
- **3 filial hardcode** — `yeni/page.tsx:294-302` (`all/neftciler/nizami/bulbul`),
  halbuki zəncir 30 filialdır. `/api/branches` var, istifadə edilmir.
  İstifadəçi modalda ham slug görür: **"📍 all"** (`promo-client.tsx:97`).
- Unsplash fallback şəkli (`page.tsx:15`).

### Yaşam döngüsü
Edit YOX · delete/arxiv YOX · deaktiv YOX → **yanlış yayınlanan indirim geri
alına bilmir**. `valid_from` yazılır, **heç oxunmur**; tarixlər sorğuda
istifadə edilmir → müddəti bitmiş promo `is_active=true` olduqca əbədi görünür.
Saat/gün qaydaları dekorativdir (happy-hour səhər 9-da da görünür).

### Şema kayması
`promotions` cədvəli `drizzle/migrations/` içində **YOXDUR** —
`scripts/create-promotions.mjs` + `alter-promotions.mjs` ilə əl ilə yaradılmış.
Növbəti `db:generate` bu cədvəli "yeni" sayacaq. `code` üçün unique index yox.

### Ölçmə: sıfır
Redemption cədvəli yox, maliyyət yox, `discount_value` **text** (toplana bilmir),
promo dövrü ilə satış arasında **tək join yox**. Modul kampanya afiş panosudur.

---

## 4. 🍔 Menü / Food Cost

- **Heç nə saxlanılmır.** `menyu-client.tsx`-də tək `fetch`/`POST` yox → fayl
  brauzerdə parse olunur, ekrana basılır, **yeniləmədə sıfırlanır** (`:92`
  düyməsi də silir). Panel bu işi düzgün edir (`panel-save` → `analytics_ingest`
  → geri oxuma + dövr arxivi); menüdə **o zəncirin heç bir halqası yox**.
  `analytics_ingest.period` artıq ay-anahtarlıdır → **migration lazım deyil**.
- **`avgFoodCost` çəkisiz arifmetik ortalamadır** (`parse-menu.ts:54`) — gündə
  3 satan qarnitür ilə 800 satan ət döner eyni çəkidə. Ekranda manşetdir
  (`menyu-client.tsx:95`) → yanlış manşet, olmamasından pisdir.
- **Üç fərqli hədəf dolaşır:** %30 (`menyu-client.tsx:91`), %33
  (`dashboard/page.tsx:283`), %25/%40 bantları (`parse-menu.ts:55-57`).
- Kolon yoxsa səssizcə `0` yazılır (`:49-50`) → cədvəldə "0.00 ₼" **real say
  kimi** görünür. Atlanan sətir sayı heç yerə yazılmır (`:45`).
- **`branch_manager` modulu heç görmür** (`menyu/page.tsx:11`, `sidebar.tsx:13`),
  amma `region_manager` şəbəkənin **bütün** maliyyət/qiymət datasını filtrsiz
  görür — halbuki Panel aynı rola cironu bilərəkdən gizlədir
  (`panel/page.tsx:54-56`: *"maliyyət sızıntısı olmasın"*). Tutarsız siyasət.
- **Dashboard Food Cost kartı `NA`** (`page.tsx:283`) — menü modulu bir say
  hesablayır, heç vaxt buraya bağlanmır.
- `/dashboard/menu`, `/admin/menu`, `/admin/menu/yeni` = köhnə **mock**
  (Unsplash şəkilləri, `alert("Məhsul yaradıldı!")`). `dashboard-access.ts`
  bunları bağlayıb, testlər qoruyur (`tests/dashboard-access.test.ts:25,40`).
  **Silinməyəcək** (AGENTS.md §2), yenidən linklənməyəcək.

### ⚠️ Ən vacib nəticə: prodmix YOXDUR
Dörd parser-in çıxışı tək-tək yoxlanıldı — **OCAQ-ın heç bir yerində məhsul
bazlı satış ƏDƏDİ yox**: `parse-daily.ts:7-18` (filial×gün×ödəniş növü),
`:228-282` (OLAP), `parse-sales.ts:16-25`, `parse-delivery.ts:10-21`,
`daily_sales` (`sales.ts:20-31`). Məhsul adı taşıyan tək struktur
`parse-menu.ts:6` və orada ədəd yox. Faylın öz şərhi bunu qeyd edir (`:3`).

→ **Kasavana-Smith matrisi (Ulduz/Bilməcə/İşçi at/İt) bugün QURULA BİLMƏZ.**
→ **Kilidi açan tək şey:** iiko-dan bir OLAP SALES export-u, qruplama
  `filial × yemək adı × ədəd × məbləğ`. iiko bunu artıq üretir (Toast-un
  "PMIX" dediyinin eynisi). Yəni əksik data əlçatmaz deyil — **sadəcə heç
  vaxt istənilməmiş bir export**.

---

## 5. 📊 Beş analiz artifact-ı → OCAQ xəritəsi

Üçü aynı motorun üç qatıdır: **Board** (təpə) → **Portföy** (zəncir prioriteti)
→ **Teşhis** (filial kartı). Üçü də aynı **kanonik eşik registry**-sini oxuyur.

### Eşik registry — artifact-da KOD olaraq yazılıdır (spesifikasiya)
```
gidisatFark  [-0.05, 0.00]     reyting      [3.2, 3.5]
deliveryPayi [0.20, 0.35]      deliveryYoy  [-0.15, 0.00]
silinmePct   [1.4, 2.0]        fazlaKadro   [0, 5]
composite    [40, 55]
// yon: + yüksək yaxşı · - alçaq yaxşı · ~ bant
sevOf(n) = trend < -0.05 ? 'crit' : trend < 0 ? 'warn' : 'good'
stake(n) = Σ f.imp   (₼ azalan sıralama)
```

### Fırsat havuzu: 575 085₼/ay · 27 diqqət filialı · 59 aksiyon

| Sürücü | ₼/ay | Filial | Bugün OCAQ verisiylə? |
|---|---|---|---|
| 📦 Delivery düşüşü | **333 451** | 20 | ✅ `parse-delivery.ts:86` + YoY |
| 💬 İtibar/servis | 63 253 | 5 | ❌ reyting feed-i yox |
| 🥤 Upsell fırsatı | 47 234 | 10 | ❌ prodmix yox |
| 💰 Labor% yüksək | 44 126 | 3 | 🟡 `staff_profiles.salary_gross` ilə təxmini |
| 💸 Komisyon riski | 42 779 | 6 | ✅ `parse-delivery.ts:90` |
| 👥 Fazla kadro | 32 500 | 5 | ❌ istasyon norması yox |
| 🗑️ Silinmə/israf | 11 743 | 10 | ❌ silinmə raporu yox |

→ **376 230₼ / 575 085₼ = %65 bugünkü veriylə raporlana bilər** (Labor%-lə ~%73).

### Filial adı eşleşməsi: PROBLEM YOX
Board/Teşhis/Portföy **OCAQ-ın kanonik Azərbaycan formalarını** işlədir.
Proqramatik yoxlama: **29 adın 29-u** `filial-map.ts:6-12`-də var, bölgə
ataması da **29/29 eyni**. `REG_SIRA` sırası `BOLGELER` sırası ilə üst-üstə.
Yalnız `Əcəmi` artifact-larda yox (OCAQ 30, artifact 29 — "like-for-like").
*Qeyd: Aylıq Trend artifact-ı İngilis transliterasiyası işlədir
(Hazi Aslanov) — `normalizeFilial` körpüsü lazımdır.*

### Aylıq Trend — bugün qurula bilər, yeni data lazım deyil
`yoyref-1.0` (filial × ay × 2025 faktiki, 165 referans) + `panel-1.0`
(dövr-dövr arxiv) → YoY-nin iki tərəfi də DB-də. Karar qaydası:
**"4+ ayda mənfi = mövsümi deyil, YAPISAL"**.
- Kronik: Mahammad Hadi 6/6 (−21%), Hazi Aslanov 6/6 (−11%), Badamdar 5/6
  (−22%), Azadliq 5/6 (−20%), Boulevard 5/6 (−13%), Inshaatchilar 4/6 (−9%)
- İyun şoku −17%, 24/28 filial, **saf trafik** (AOV sabit → qiymət deyil)
- ⚠️ İki tələ artifact-da qeyd edilib, kodda qorunmalı: Mart 2025 30 gün /
  2026 31 gün (gün normalizasyonu); İyun 2025 ilin zirvəsi (517K) → yüksək baza.

### Saha Nəzarət — beşin ən təmiz oturanı
77 kontrol · 10 blok · 24 P0 · 15 günlük dövr. **Rapor deyil, ölçü alətidir** —
heç bir Excel onu üretmir, bölgə müdiri filialda doldurur. `checklists` şeması
(`branch_id` + `business_date` + `score_pct` + `items_json` + `idempotency_key`)
bunu **neredeyse birebir** taşıyır. **Motor bu oturumda yazıldı** (`c5f0576`).

### ⚠️ Sənədə keçirilməli xəbərdarlıq
Portföy künyəsi: *"₼ etkiler fırsat böyüklüyü **təxminidir** (aylıq), kəsin
öhdəlik deyil — önceliklendirme üçün."* → OCAQ səhifəsinə **eynən** keçməlidir,
əks halda 575 085₼ hədəf kimi oxunar.

### Mimari xəbərdarlıq
Artifact-lar `atomic_purchases` adlı **ayrı DB**-dən beslənir → hazırda iki
gerçəklik kaynağı var. `SYSTEM-TREE.md`-nin *"altın veri kuralı"* ilə ziddiyət.
Hədəf: **OCAQ tək kaynak, motor yalnız keçmiş referansı.**

---

## 6. 🎯 Ən yüksək kaldıraçlı tək dəyişiklik

> **Günlük yükləməni normalize fact cədvəlinə yaz:**
> `analytics_daily_fact(tenant_id, branch_id, filial, business_date, payment_type, amount)`
> — günlük idempotent upsert. JSON blob-a gömməyi burax.

**Niyə:**
1. Hazırda bütün parse nəticəsi tək JSON string-ə gömülür
   (`panel-save/route.ts:25` → `network: blob`), dövr başına bir sətir. Ona görə
   "keçən il aynı ay" sualı **fayldan** cavablanır. Fact cədvəli olsa YoY, trend,
   gidişat, plan-tutturma, satış təxmini + CI **OCAQ-ın öz birikmiş verisindən**
   çıxar.
2. **Aylıq Trend %100 bu cədvəldən doğar** ("4+ ay mənfi" = SQL window function).
   Board-un 5 tile-ı, Teşhis-in `gidisat`/`Delivery YoY`, Portföy-ün iki böyük
   sürücüsü = **havuzun %65-i**.
3. **Sıfır yeni fayl, sıfır yeni insan işi** — bugün yüklənən faylı fərqli yerə
   yazmaq. `parseDaily` (`parse-daily.ts:112-170`) lazım olan hər sahəni artıq
   üretir. Parser-ə toxunulmur.
4. ⚠️ **`payment_type` boyutu MƏCBURİDİR.** `daily_sales` (`sales.ts:20-31`)
   `branch_id × sale_date × amount` saxlayır — **kanal ayrımı YOX**. Yalnız
   toplam ciro saxlanılsa ₼-in %65-i itir (delivery, komisyon riski, kasa-banka
   mutabakatının hamısı o kolondan gəlir).
5. Sonrakı addımlar aynı desenin qardaşıdır: `filial×gün×məhsul`,
   `filial×ay×silinmə`, `filial×ay×kadro`.

**İkinci sıra:** günlük export-u **məhsul-sətir** granülerliğinə qaldır
(`filial × gün × məhsul × ədəd × məbləğ × ödəniş növü`) → Upsell (47 234₼),
`Ort.Çek`/`Çek Sayı` (dashboard-da `—`), Ulduz/Bilməcə, çəkili Food Cost.

---

## 7. ❓ Yalnız istifadəçinin cavablaya biləcəyi 5 sual

Bunlar cavablanmadan Teşhis/Board/Portföy səhifələri **qurulmamalıdır** — əks
halda rəqəm uydurulur (`PRODUCT-COMPLETION-CONTRACT.md` qadağan edir).

1. **`composite` (KPI skor) formülü nədir?** Üç artifact 0-100 skor göstərir,
   eşiyi yazılı (`[40, 55]`), **formülü heç birində yox**. Motor kodunda olmalıdır
   (`analitik_kpi.py`).
2. **`imp` (₼ etki) formülləri.** Delivery və Upsell türetilebilir görünür; amma
   İtibar/servis (19 000 / 15 000 / 10 000) və Fazla kadro (~500₼ × nəfər)
   **sabit tarif** kimidir. Registry-də açıq cədvəl olaraq saxlanılmalıdır.
3. **`fazla` sahəsi 29 filialın 29-unda boşdur** — amma sürücü 5 filialda
   `+19/+16/+12/+10/+8 nəfər` tetikliyor. Board-un `Fazla` kolonu da tamamilə
   `—`, halbuki Domenlər kartı "24 filial fazla ~148 nəfər" deyir. İki fərqli
   besleme, yoxsa bug?
4. **Reyting bant sınırları** — `bant Kritik` / `bant Təhlükə` kesme noktası
   yazılı deyil (yalnız `REG.reyting = [3.2, 3.5]`).
5. **`Əcəmi` niyə 29-un xaricində?** like-for-like qərarı, yoxsa data boşluğu?

**Lazım olan fayllar:** `Shaurma_N1_Saha_Nezaret_Matrisi.xlsx` (77 kontrol
mətni + hansı 24-ü P0), `filial_teshis.py`, `analitik_kpi.py`.

---

## 8. Prioritet sırası (tövsiyə)

**P0 — bloklayıcı, ucuz**
1. Promosyon: route-u açmadan ƏVVƏL RBAC + audit + rate limit + saxta QR +
   real filial siyahısı + versiyalı migration (bir PR-də). Sonra route açılsın.
2. `scripts/migrate-prod.mjs` repoya alınsın.
3. Mobil borc: 5 modal `maxHeight` (repodaki hazır deseni yay) + 3 cədvəl
   sarmalayıcısı + KXT filial seçicisinin böyüdülməsi.
4. Menü: `menu-1.0` ingest ilə kalıcılıq (migration lazım deyil) + çəkisiz
   ortalamanın dürüst etiketlənməsi + tək food cost hədəfi.

**P1 — kaldıraç**
5. **Fact cədvəli** (`payment_type` ilə) — havuzun %65-i. Migration → snapshot
   + açıq təsdiq lazım.
6. `src/lib/analytics/registry.ts` — artifact-daki `REG` + `sev()` + `sevOf()`
   birebir port. Board/Teşhis/Portföy üçün tək kaynak.
7. **Aylıq Trend** səhifəsi — mövcud veriylə tam qurulur.
8. KXT `localStorage` qaralaması (vardiya ortasında 100 maddə itməsin).

**P2 — data gözləyir**
9. Prodmix ingest → Menü mühəndisliği + Upsell + çəkili food cost + promo ölçmə.
10. Teşhis → Portföy → Board (7 sualdan sonra).
11. Saha Nəzarət kataloqu + şema + API + səhifə (xlsx gözlənilir).

---

## 9. Sənədlərdə tapılan səhvlər (düzəldilməli)

| Sənəd | Səhv |
|---|---|
| `OCAQ-MIMARI-DEPLOY-FAZ3.md` | ✅ düzəldildi (`d2c1653`) |
| `CHANGELOG.md` | "Promosyonlar mock-dan gerçəyə" — modul erişilə bilmir |
| `OCAQ-MIMARI-DEPLOY-FAZ3.md` | "✅ Promosyonlar (qismən)" — aynı problem |
| `FORM-REALITY-AUDIT.md` (16 iyul) | promosyon/menü bölmələri köhnəlmiş |
| `OCAQ-DURUM-VE-YAPILACAKLAR.md` (18 iyul) | "Production = ocaq-app.vercel.app, dal = main" — domen artıq `ocaq.dkagency.com.tr` |

---

_Denetim: 4 paralel kod agenti (promosyon · menü · mobil · ümumi) + 5 artifact
analizi. Salt-oxunur; bu sənəd yazılana qədər heç bir fayl dəyişdirilməmişdi._
